# Debug This Issue Prompt

Use the debugger agent.

This command failed:

```txt
[paste command]
```

Output:

```txt
[paste error]
```

Diagnose the root cause first. Then suggest the smallest safe fix. Do not rewrite unrelated files.
